[project]
name = "fusion360-mcp"
version = "1.0.0"
description = "Model Context Protocol server for Autodesk Fusion 360"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
]

[tool.uv]
package = false
